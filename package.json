{
  "name": "cieloefata-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.development.local --env-file-if-exists=/vercel/share/.env.project server.js",
    "start": "node server.js",
    "check": "node --check api/config.js && node --check api/exegesis.js && node --check api/biblia.js"
  },
  "dependencies": {
    "@google/genai": "^1.29.1",
    "@supabase/supabase-js": "^2.57.4"
  },
  "engines": {
    "node": ">=20"
  }
}
